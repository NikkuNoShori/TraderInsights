import React, {
  createContext,
  useContext,
  useState,
  useEffect,
  useRef,
  useCallback,
  useMemo,
  ReactNode,
  ReactElement,
  FC,
  ComponentType,
  ErrorInfo,
  Suspense,
  lazy,
  Component,
  memo,
  type PropsWithChildren,
  type ChangeEvent,
  type FormEvent,
  type SetStateAction,
  type RefAttributes,
  type JSXElementConstructor,
} from "react";

// Re-export React
export default React;

// Export hooks
export {
  createContext,
  useContext,
  useState,
  useEffect,
  useRef,
  useCallback,
  useMemo,
  Suspense,
  lazy,
  Component,
  memo,
};

// Export types
export type {
  ReactNode,
  ReactElement,
  FC,
  ComponentType,
  ErrorInfo,
  PropsWithChildren,
  ChangeEvent,
  FormEvent,
  SetStateAction,
  RefAttributes,
  JSXElementConstructor,
};
