import React, {
  createContext,
  useContext,
  useState,
  useEffect,
  useRef,
  useCallback,
  ReactNode,
  ReactElement,
  FC,
  ComponentType,
  ErrorInfo,
  Suspense,
  lazy,
} from "react";

export type { ReactNode, ReactElement, FC, ComponentType, ErrorInfo };

export {
  React,
  createContext,
  useContext,
  useState,
  useEffect,
  useRef,
  useCallback,
  Suspense,
  lazy,
};

export default React;
