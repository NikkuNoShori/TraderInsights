import React, {
  createContext,
  useContext,
  useState,
  useEffect,
  useRef,
  useCallback,
  useMemo,
  ReactNode,
  ReactElement,
  FC,
  ComponentType,
  ErrorInfo,
  Suspense,
  lazy,
  type PropsWithChildren,
  type ChangeEvent,
  type FormEvent,
} from "react";

// Export types
export type {
  ReactNode,
  ReactElement,
  FC,
  ComponentType,
  ErrorInfo,
  PropsWithChildren,
  ChangeEvent,
  FormEvent,
};

// Export hooks and utilities
export {
  React as default,
  createContext,
  useContext,
  useState,
  useEffect,
  useRef,
  useCallback,
  useMemo,
  Suspense,
  lazy,
};
